extends ../layouts/main-layout

block content

    main.centered
        h1 #{product.title}
        div
            img(src=`${product.imageUrl}`)
        h2 #{product.price}
        p #{product.description}


